//! `bash` tool — execute a shell command in the workspace cwd. Real
//! impl lands in Task 24 of Plan 1.

/// Bash subprocess tool. Implements the [`crate::Tool`] trait in
/// Task 24.
pub struct BashTool;
