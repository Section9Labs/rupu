//! `read_file` tool — full-file read with line-numbered output. Real
//! impl lands in Task 19 of Plan 1.

/// Reads a file relative to the workspace root. Implements the
/// [`crate::Tool`] trait in Task 19.
pub struct ReadFileTool;
