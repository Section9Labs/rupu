import SwiftUI
import RupuShell

@main
struct RupuApp: App {
    var body: some Scene {
        WindowGroup {
            Text("rupu — foundation scaffold")
                .frame(minWidth: 1150, minHeight: 760)
        }
    }
}
